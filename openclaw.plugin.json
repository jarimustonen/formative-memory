{
  "id": "memory-associative",
  "name": "Memory (Associative)",
  "kind": "memory",
  "description": "Biologically-inspired associative memory with consolidation, temporal awareness, and retrieval-based strengthening.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "embedding": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "provider": {
            "type": "string",
            "default": "auto"
          },
          "model": {
            "type": "string"
          }
        }
      },
      "dbPath": {
        "type": "string"
      },
      "autoCapture": {
        "type": "boolean"
      },
      "autoRecall": {
        "type": "boolean"
      }
    }
  },
  "uiHints": {
    "embedding.provider": {
      "label": "Embedding Provider",
      "placeholder": "auto",
      "help": "Embedding provider to use. \"auto\" selects the best available provider based on configured API keys."
    },
    "embedding.model": {
      "label": "Embedding Model",
      "help": "Override the provider's default embedding model"
    },
    "dbPath": {
      "label": "Database Path",
      "placeholder": "~/.openclaw/memory/associative",
      "advanced": true
    },
    "autoCapture": {
      "label": "Auto-Capture",
      "help": "Automatically capture all conversations for consolidation"
    },
    "autoRecall": {
      "label": "Auto-Recall",
      "help": "Automatically inject associated memories into context"
    }
  }
}
